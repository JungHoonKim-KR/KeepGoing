spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/keepgoing?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: root
    password: kik0811012
  #MyBatis 설정
mybatis:
  type-aliases-package: keepgoing.demo.domain.diet.entity, keepgoing.demo.domain.member.entity, keepgoing.demo.domain.ai.entity
  mapper-locations: classpath:/mappers/**/*.xml
  configuration.map-underscore-to-camel-case: true
#  security:
#    oauth2:
#      client:
#        registration:
#          google:
#            client-id: ${GOOGLE_CLIENT_ID}
#            client-secret: ${GOOGLE_CLIENT_SECRET}
#            redirect-uri: ${GOOGLE_REDIRECT_URI}
#            scope:
#              - email
#              - profile
